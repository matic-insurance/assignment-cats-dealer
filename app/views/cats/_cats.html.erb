
<% if @cats.size > 0 %>

<table class=" table table-striped">
  <thead>
  <tr>
    <th>Type</th>
    <th>Price</th>
    <th>Location</th>
    <th>Image</th>
  </tr>
  </thead>

  <tbody>
  <% @cats.each do |cat| %>
      <tr>
        <td><%= name(cat['name']) %></td>
        <td><%= price(cat['price']) %></td>
        <td><%= cat['location'] %></td>
        <td><%= image_tag(cat.fetch('image', 'no_cat_icon.png'), width: '30px', alt:'no icon') %></td>
      </tr>
  <% end %>
  </tbody>
</table>
<% else %>
  <p>
    Sorry, no cats for your location
  </p>
<% end %>