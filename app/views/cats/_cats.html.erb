
<% if @cats.size > 0 %>

<table class=" table table-striped">
  <thead>
  <tr>
    <th>Breed</th>
    <th>Price</th>
    <th>Location</th>
    <th>Image</th>
  </tr>
  </thead>

  <tbody>
  <% @cats.each do |cat| %>
      <tr>
        <td><%= cat['breed'] %></td>
        <td><%= cat['price'] %></td>
        <td><%= cat['location'] %></td>
        <td><%= image_tag(cat.fetch('image_src', 'no_cat_icon.png'), width: '30px', alt:'no icon') %></td>
      </tr>
  <% end %>
  </tbody>
</table>
<% else %>
  <p>
    Sorry, no cats for your location
  </p>
<% end %>